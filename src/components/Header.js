const Header = () => {
    return (
        <header
            style={{ borderBottom: '1px solid #ececec', marginBottom: '25px' }}
        >
            <div>
                <h1>로그인</h1>
            </div>
        </header>
    );
};

export default Header;
